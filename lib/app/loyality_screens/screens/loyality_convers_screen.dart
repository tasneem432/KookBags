import 'package:flutter/material.dart';


class LoyalityConvertScreens extends StatelessWidget {
  static const pageRoute = "/loyality-convert-screen";
  const LoyalityConvertScreens({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text("This is loyality convert screen")
        ],
      ),
    );
  }
}
